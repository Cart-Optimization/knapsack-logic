# Runtime dependencies (pinned for reproducible production builds).
# Dev/test-only deps live in requirements-dev.txt and are NOT shipped in the image.
mcp==1.27.2
httpx==0.28.1
fastapi==0.136.3
uvicorn[standard]==0.49.0
itsdangerous==2.2.0
